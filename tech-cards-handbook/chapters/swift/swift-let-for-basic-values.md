# Swift 基础值优先用 `let`

**问题**：什么时候用 `let`，什么时候用 `var`？

**要点**：

- `let` 声明常量，赋值后不能改。
- `var` 声明变量，可以重新赋值。
- 默认用 `let`，只有确实需要变化时用 `var`。

**示例**：

```swift
let name = "Ada"
let age: Int = 30

var score = 100
score = 99
```

**坑**：过度使用 `var` 会让状态变化边界不清楚，调试时更难判断谁改了值。

**检查**：这个值声明后是否真的会变？不会就用 `let`。
